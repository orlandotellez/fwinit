# Copia este archivo a `.env` y ajusta los valores.
# Vite expone solo las variables con prefijo VITE_ al cliente.

# URL base del API. Si no se define, api-config.ts usa su DEFAULT_API_URL
# (http://localhost:3000/api/v1) en desarrollo.
VITE_API_URL=http://localhost:3000/api/v1